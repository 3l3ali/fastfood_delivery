continue
@order.save
@order
session[:items_ids].each { |item| @order.items << Item.find(item) }
continue
@order.errors
@order.save
@order
session[:items_ids].each { |item| @order.items << Item.find(item) }
