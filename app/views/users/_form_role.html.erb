<% if current_user.manager? %>
  <%= form_tag users_path, method: :get do %>
    <%= select_tag :role, options_for_select(User.roles_for_select, params[:role]) %>
    <%= submit_tag "search" %>
  <% end %>
<% end %>

