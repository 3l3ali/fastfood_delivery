<% if current_user.manager? %>
  <%= simple_form_for @item do |f| %>
    <%= f.input :name %>
    <%= f.input :category, collection: Item.categories.keys %>
    <%= f.input :price %>
    <%= f.input :description %>
    <%= f.button :submit %>
  <% end %>
<% end %>
